.log
  %strong [#{log.created_at.in_time_zone(current_user.time_zone).strftime('%F %H:%M')}]
  .log-resub
    %p
      %strong
        %i.fa.fa-dollar
      %strong= log.username
      just resubscribed with a
      %strong= sub_plan_display(log.sub_plan)
      sub for
      %strong #{log.month} months in a row.
