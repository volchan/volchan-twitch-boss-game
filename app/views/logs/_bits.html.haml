.log
  %strong [#{log.created_at.in_time_zone(current_user.time_zone).strftime('%F %H:%M')}]
  .log-bits
    %p
      %strong
        %i.fa.fa-dollar
      %strong= log.username
      just donated
      %strong= log.bits_amount
      bits.
