$('.notification-container').prepend("<%= j render 'shared/flashes' %>");
closeNotification();
animateNotifications('slideInRight');

var linksContainer = document.getElementById("goals-show-links");
var links = "<%= j render 'dashboard/dashboards/goals_links', sub_goal: Goal.sub_goal.in_progress.first, bits_goal: Goal.bits_goal.in_progress.first %>";
linksContainer.innerHTML = links;

if ("<%= @goal.g_type %>" === "sub_goal") {
  var form = "<%= j render 'dashboard/goals/sub_goal_form', sub_goal: @goal %>";
  var subGoalContainer = document.querySelector(".sub-goal-form");

  subGoalContainer.innerHTML = form;
} else {
  var form = "<%= j render 'dashboard/goals/bits_goal_form', bits_goal: @goal %>";
  var bitsGoalContainer = document.querySelector(".bits-goal-form");

  bitsGoalContainer.innerHTML = form;
}

$('#new-goal-modal').modal('hide');
